import BankInfoCard from '@/shared/components/BankInfoCard/BankInfoCard';

const ComponentTest = () => {

  return (
      <div
        style={{
          display: 'flex',
          padding: '1rem',
          gap: '0.6rem',
        }}
      >
        <BankInfoCard />

      </div>
  );
};

export default ComponentTest;
